Make reset password emails autofill the token
Add more admin views - sort by wishes, sort families by size, etc
More detailed audit logs - maybe audit database info so admins can easily scroll through a specific user's actions?
DONOR - Add donor users - able to see families in tiled cards, filter by size (maybe other filters?), mark families as claimed either by donating money or marking them as claimed and then using their wish list to get wishes. They'd get confirmation and reminder emails.
EVENTUALLY - Automated database backups
EVENTUALLY - Rate-limiting referrer email sending to family_limit per day just in case. Also maybe a once-per-address-per-day limit, or just once-per-address which requires an admin to reset if lost.

Things that need clarification:
DONOR - Which fields shown in donor-view cards? Name, city, size, bio, family_wish? family ages?
DONOR - Filter granularity? Family size, contains a person with age in range?
Wish sizes; included in wish instructions or add an extra field? Instructions make more sense to avoid bloating fields and adding confusion + if they can't follow instructions anyway then the extra field would become a mess
Add some sort of "purchased" marker for each gift. Maybe a date and purchased_by? Maybe a general "Admin/referrer note" section? Or just admin note?
Approval process for referrers to approve their families' wishes and maybe additional admin approval
