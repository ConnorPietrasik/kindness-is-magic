[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
# slowapi uses asyncio.iscoroutinefunction which is deprecated in Python 3.14+
# (removed in 3.16). Not our code — filter until slowapi updates.
filterwarnings =
    ignore:.*asyncio.iscoroutinefunction.*:DeprecationWarning
